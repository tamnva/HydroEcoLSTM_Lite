# API Reference

Automatic API pages can be generated with `mkdocstrings`.

To include module documentation inline use the following directive in markdown:

```
::: hydroecolstm_lite.model_run
```

You can open the site locally with:

```bash
mkdocs serve
```

and the API pages will be populated automatically if `mkdocstrings` is installed.
