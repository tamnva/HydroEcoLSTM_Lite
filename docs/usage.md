# Usage

## Quick start

1. Install the package in editable mode:

```bash
pip install -e .
```

2. Edit `examples/camels_ch/config.yml` to point to your data files.

3. Run the example script:

```bash
python examples/camels_ch/main.py
```

## Programmatic

See `README.md` for a short programmatic example using `run_config`.
